(define f
    (! (n) (if (= n 0)
                1
                (* n (f (- n 1))))))

(define defmacro
    (! (ident args body) 
        (define ident 
            (! args body))))

(defmacro add (a b) (+ a b))
(defmacro factorial (n) (if (= n 0)
                            1
                            (* n (factorial (- n 1)))))
